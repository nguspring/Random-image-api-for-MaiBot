# Random-image-api-for-MaiBot 插件

> 当前插件市场发布版本：**v1.0.2**

> 基于 [Random-image-api](https://github.com/inliver233/Random-image-api) 的 MaiBot 随机 Pixiv 图片插件

## 📖 简介

这个插件可以从 Random-image-api 拉取 Pixiv 随机图片，支持标签筛选、多图获取、AI 过滤、尺寸筛选、R18 权限控制和图片压缩发送。

这次 `v1.0.2` 重点更新了两件事：

- 🛡️ **命令冷却改成全局冷却**：一个人触发后，其他人也会一起进入冷却
- 🔞 **R18 新增白名单 / 黑名单模式**：默认是黑名单模式，也就是全局允许 R18，但名单里的群号 / QQ 号会自动回退成普通图

## ✨ 功能特性

- 🎨 **Pixiv 图源** — 所有图片来自 Pixiv
- 📦 **多图获取** — 一次最多获取 10 张图片
- 🔍 **丰富筛选** — 支持标签、画师 UID、方向、收藏数、浏览数、宽高、像素数筛选
- 🤖 **AI 控制** — 默认排除 AI 图，也可以临时切成只看 AI
- 🔞 **R18 权限控制** — 支持总开关、白名单模式、黑名单模式
- 🛡️ **全局冷却** — 防止一群人轮流刷命令
- 📦 **两种发送方式** — 合并转发 / 逐条发送
- 🚀 **快速发图** — 跳过 VLM 识图，减少卡顿
- 📐 **智能压缩** — 自动压缩图片再发送，明显提升发图速度

## 📦 安装

把插件目录放进：

```text
MaiBot/plugins/random_image_api_base64/
├── _manifest.json
├── plugin.py
├── config.toml      # 首次启动后自动生成
└── README.md
```

然后：

1. 重启 MaiBot
2. 等系统自动生成 `config.toml`
3. 把 `plugin.enabled = true` 打开
4. 按需修改配置
5. 再重启一次 MaiBot

> 插件依赖 `Pillow`，框架会按插件声明自动安装。

## ⚙️ 配置说明

下面是和当前代码一致的核心配置说明。

### 1）基础开关

```toml
[plugin]
enabled = true
config_version = "1.0.2"

[components]
enable_command = true
```

### 2）API 配置

```toml
[api]
base_url = "https://i.mukyu.ru"
timeout = 30
```

### 3）功能配置

```toml
[features]
default_num = 1
max_num = 10

# 全局冷却秒数，0 表示不限制
cooldown_seconds = 10

# R18 总开关
# false: 所有人都会自动回退普通图
# true: 继续看 r18_mode 和名单配置
allow_r18 = true

# R18 名单模式
# blacklist: 默认模式，整体允许 R18，名单里的群号 / QQ 号不能用 R18
# whitelist: 只有名单里的群号 / QQ 号能用 R18
r18_mode = "blacklist"

# QQ 号名单
# 支持换行、空格、英文逗号、中文逗号分隔
r18_user_id_list = """
123456789
987654321
"""

# 群号名单
# 支持换行、空格、英文逗号、中文逗号分隔
r18_group_id_list = """
123456789
987654321
"""

exclude_ai = true
default_strategy = "random"
quality_samples = 10
use_forward_message = true
```

### 4）图片筛选配置

```toml
[filter]
min_width = 0
min_height = 0
min_pixels = 0
```

### 5）图片压缩配置

```toml
[image_compress]
enabled = true
max_image_width = 1280
jpeg_quality = 85
```

## 🔞 R18 白名单 / 黑名单怎么理解

### 黑名单模式（默认）

```toml
allow_r18 = true
r18_mode = "blacklist"
```

含义：

- 默认所有地方都可以请求 R18
- 但如果当前 **群号** 或 **QQ 号** 在名单里，就不能用 R18
- 被拦住时不会报错，而是**自动回退成非 R18 图片**

适合：大部分群都能开，只有少数群 / 少数人禁用 R18。

### 白名单模式

```toml
allow_r18 = true
r18_mode = "whitelist"
```

含义：

- 只有名单中的 **群号** 或 **QQ 号** 才能请求 R18
- 其他地方即使输入 `r18` 或使用“涩图”触发词，也会自动回退成普通图

适合：只想在少数指定群或指定用户环境下开放 R18。

### 总开关关闭

```toml
allow_r18 = false
```

含义：

- 不看白名单还是黑名单
- 所有 R18 请求都直接回退为普通图

## 🎮 使用方法

### 基础命令

```text
随机图片
来张图 3
随机图片 help
```

### 支持的常用触发词

```text
随机图片 / 来张图 / 来点图 / 来点图片 / 来张图片
涩图 / 来张涩图 / 来点涩图 / 随机涩图 / setu / 来张setu / 来点setu
```

### 标签筛选

```text
随机图片 #原神
随机图片 #碧蓝航线 #巨乳
随机图片 -#AI
随机图片 #原神 -#AI
```

### AI 控制

```text
随机图片 noai
随机图片 ai
```

### R18 用法

```text
随机图片 r18
来张涩图
来张涩图 3 #FGO
```

说明：

- “涩图 / setu”这类触发词会自动请求 R18
- 普通触发词也能通过加 `r18` 参数来请求 R18
- 如果当前群号或 QQ 号不允许 R18，会自动回退成普通图

### 其他筛选参数

```text
随机图片 横屏
随机图片 竖屏
随机图片 方形
随机图片 uid:12345678
随机图片 收藏>1000
随机图片 浏览>5000
随机图片 宽度>1920
随机图片 高度>1080
随机图片 像素>2073600
随机图片 quality
随机图片 random
```

### 组合示例

```text
来张图 3 #原神 noai 横屏
随机图片 5 #碧蓝航线 收藏>1500
随机图片 #白丝 -#AI uid:12345678
来张涩图 #FGO quality
来张涩图 3 收藏>2000 横屏
```

## 📊 参数速查表

| 参数 | 作用 | 示例 |
|------|------|------|
| `数字` | 获取数量（1-10） | `3` |
| `#标签` | 包含标签 | `#原神` |
| `-#标签` | 排除标签 | `-#AI` |
| `r18` | 请求 R18 | `随机图片 r18` |
| `noai` | 只要非 AI 图 | `noai` |
| `ai` | 只要 AI 图 | `ai` |
| `横屏/竖屏/方形` | 方向筛选 | `横屏` |
| `uid:数字` | 指定画师 | `uid:12345678` |
| `收藏>数字` | 收藏下限 | `收藏>1000` |
| `浏览>数字` | 浏览下限 | `浏览>5000` |
| `宽度>数字` | 最小宽度 | `宽度>1920` |
| `高度>数字` | 最小高度 | `高度>1080` |
| `像素>数字` | 最小像素 | `像素>2073600` |
| `quality/random` | 切换策略 | `quality` |
| `help` | 查看帮助 | `help` |

## ⚠️ 注意事项

- 插件默认安装后是关闭的，要手动打开 `enabled = true`
- 全局冷却默认 10 秒，一个人触发后其他人也要等
- R18 名单识别用的是**群号和 QQ 号**，不是聊天流 ID
- 如果 R18 被禁止，不会报权限错误，而是自动回退到普通图
- 所有图片版权归原作者所有，请勿用于违法或商业滥用场景

## 🔧 故障排除

### 获取不到图片

1. 检查 `api.base_url` 是否可访问
2. 检查日志里有没有超时或网络报错
3. 尝试减少筛选条件
4. 先用最简单的 `随机图片` 测试基础功能

### 说了“涩图”却没出 R18

按下面顺序检查：

1. `allow_r18` 是否为 `true`
2. 当前 `r18_mode` 是不是把你限制住了
3. 当前群号是否在 `r18_group_id_list`
4. 当前 QQ 号是否在 `r18_user_id_list`

### 发图太慢

可以尝试：

1. 增大 `api.timeout`
2. 保持 `image_compress.enabled = true`
3. 降低 `image_compress.max_image_width`

## 📝 更新日志

### v1.0.2

- ✨ 新增全局冷却，改为所有用户共享同一个命令冷却
- ✨ 新增 R18 白名单 / 黑名单模式
- ✨ R18 名单支持直接填写群号和 QQ 号
- ✨ 被限制 R18 时自动回退为非 R18 图片
- 📝 重写 README，配置说明和当前代码保持一致

### v1.0.1

- 🐛 修复插件主类元信息使用 `@property` 导致的加载失败问题

### v1.0.0

- 🎉 插件市场正式发布版本

---

**请合理使用本插件。**
